import { createWebClient } from "./lib/appwriteWeb";

const getData = {

}
const postData = {
  toDoList: async () => {
    
    const {tablesDB}= createWebClient()
  }
  
}
const putData = {
  
}

const deleteData = {
  
}
export {getData, postData, putData, deleteData}